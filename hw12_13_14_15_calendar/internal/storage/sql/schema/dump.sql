CREATE TABLE PATIENT (
    ID SERIAL PRIMARY KEY,
    PERS_NUMBER VARCHAR(14),
    MED_CARD    VARCHAR(16),
    SURNAME VARCHAR(255),
    FIRST_NAME VARCHAR(255),
    SECOND_NAME VARCHAR(255),
    SEX BOOLEAN,
    BIRTHDATE DATE,
    CREATED_AT TIMESTAMP
);
